import 'package:flutter/material.dart';

class ArticalCard extends StatelessWidget {
  const ArticalCard({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}