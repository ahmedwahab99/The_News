class Article {
  String imageURL, articleURL, title, text, content, source;
  Article(
      {required this.imageURL,
      required this.articleURL,
      required this.source,
      required this.content,
      required this.text,
      required this.title});
}
